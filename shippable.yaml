language: python
python:
 - "2.7"

services: 
 - redis 
install: pip install -r requirements.txt

script:
 - python sample.py
notifications:
  email:
    - exampleone@org.com
#build_image: drydock/u14pytpls:prod
branches:
 only:
   - master
   - test
   - stable
